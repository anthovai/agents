# เปรียบเทียบโมเดลสำหรับผู้ช่วยระบบ Indorama

รันเมื่อ **2026-08-18 · Ollama บนเครื่องพัฒนา · 3 รอบต่อโมเดล** · คำถามในเรื่อง 10 ข้อ · คำถามนอกเรื่อง 4 ข้อ

สร้างโดย `python bench.py` — รันซ้ำได้ ตัวเลขในไฟล์นี้ไม่ใช่ตัวเลขที่พิมพ์มือ

## สรุป

| โมเดล | ตอบถูก (ต่ำ–สูง) | รอบ | โดน guard ทิ้ง | ผิดพลาดอื่น | มัธยฐาน (วิ) | กันนอกเรื่อง |
|---|---|---|---|---|---|---|
| `qwen2.5:7b-instruct` | **8–9/10** | 3 | 0 | 0 | 7.8 | 4/4 |
| `llama3.1:8b` | **7/10** | 3 | 2 | 1 | 9.2 | 4/4 |
| `gemma4:latest` | **7–9/10** | 3 | 1 | 4 | 43.8 | 4/4 |

**ตอบถูก** = ตอบสำเร็จ *และ* มีข้อเท็จจริงที่ตรวจได้อยู่ในคำตอบ (ยอดรวมจาก digest,
ชื่อคอลัมน์ที่มีจริง) — ไม่ใช่แค่ตอบออกมาได้

**โดน guard ทิ้ง** = คำตอบถูก `app.guard` ปฏิเสธเพราะมีชื่อหรือตัวเลขที่ไม่ได้อยู่ใน
material นี่คือผลลัพธ์*ที่ถูกต้อง*สำหรับคำตอบที่ผิด และยังเป็นความล้มเหลวที่ผู้ใช้เจอ —
เขาถามแล้วได้ข้อความปฏิเสธ โมเดลที่โดนทิ้งบ่อยไม่ใช่โมเดลที่ปลอดภัย แต่คือโมเดลที่
ใช้ไม่ได้ซึ่งถูกกันไว้ด้วยบริการที่ปลอดภัย

**กันนอกเรื่อง** ตัดสินใน `app.scope` ก่อนเรียกโมเดล จึงเท่ากันทุกโมเดลโดยการออกแบบ
ถ้าคอลัมน์นี้ไม่เต็มแปลว่ามีบั๊ก ไม่ใช่แปลว่าโมเดลแย่

**ช่วง ต่ำ–สูง คือสิ่งที่ต้องอ่านก่อนอย่างอื่น** โมเดลเดียวกัน prompt เดียวกัน ให้คะแนน
ต่างกันได้ระหว่างรอบ ความต่างหนึ่งคะแนนระหว่างสองการตั้งค่าจึงไม่ใช่หลักฐานของอะไรเลย
ถ้ารันด้วย `--repeat 1` ช่วงจะยุบเป็นตัวเลขเดียวที่ดูหนักแน่นกว่าความจริง

## รายข้อ

### `qwen2.5:7b-instruct`

| คำถาม | ต้องมี | ผล | วิ |
|---|---|---|---|
| มีตารางทั้งหมดกี่ตาราง | `192` | ถูก | 15.0 |
| ตารางไหนบ้างที่มีข้อมูลอ่อนไหว | `26` | ถูก | 33.6 |
| which tables hold personal data | `26` | ถูก | 30.8 |
| route ทั้งหมดมีกี่ route | `427` | ตอบได้ แต่ไม่มีข้อเท็จจริงที่ตรวจ | 4.8 |
| tbl_company มีคอลัมน์อะไรบ้าง | `default_user_password` | ถูก | 19.4 |
| ตาราง tbl_certificate เก็บอะไร | `cert_fullname` | ถูก | 19.5 |
| Authorization_Token.php มีเมธอดอะไรบ้าง | `generateAccessToken` | ถูก | 5.9 |
| ci_sessions มีคอลัมน์อะไร | `ip_address` | ถูก | 6.8 |
| ความสัมพันธ์ระหว่างตารางมีอะไรบ้าง | `28` | ถูก | 35.6 |
| route ของ Channels มีอะไรบ้าง | `channels` | ถูก | 6.0 |

### `llama3.1:8b`

| คำถาม | ต้องมี | ผล | วิ |
|---|---|---|---|
| มีตารางทั้งหมดกี่ตาราง | `192` | ถูก | 17.4 |
| ตารางไหนบ้างที่มีข้อมูลอ่อนไหว | `26` | ถูก | 29.9 |
| which tables hold personal data | `26` | `llm_timeout` | 92.4 |
| route ทั้งหมดมีกี่ route | `427` | ตอบได้ แต่ไม่มีข้อเท็จจริงที่ตรวจ | 6.4 |
| tbl_company มีคอลัมน์อะไรบ้าง | `default_user_password` | ถูก | 20.8 |
| ตาราง tbl_certificate เก็บอะไร | `cert_fullname` | ถูก | 9.5 |
| Authorization_Token.php มีเมธอดอะไรบ้าง | `generateAccessToken` | `ungrounded_answer` | 14.5 |
| ci_sessions มีคอลัมน์อะไร | `ip_address` | ถูก | 5.2 |
| ความสัมพันธ์ระหว่างตารางมีอะไรบ้าง | `28` | ถูก | 7.6 |
| route ของ Channels มีอะไรบ้าง | `channels` | ถูก | 5.9 |

### `gemma4:latest`

| คำถาม | ต้องมี | ผล | วิ |
|---|---|---|---|
| มีตารางทั้งหมดกี่ตาราง | `192` | ถูก | 48.4 |
| ตารางไหนบ้างที่มีข้อมูลอ่อนไหว | `26` | `ungrounded_answer` | 49.7 |
| which tables hold personal data | `26` | ถูก | 50.7 |
| route ทั้งหมดมีกี่ route | `427` | ถูก | 35.5 |
| tbl_company มีคอลัมน์อะไรบ้าง | `default_user_password` | ถูก | 47.6 |
| ตาราง tbl_certificate เก็บอะไร | `cert_fullname` | ตอบได้ แต่ไม่มีข้อเท็จจริงที่ตรวจ | 52.0 |
| Authorization_Token.php มีเมธอดอะไรบ้าง | `generateAccessToken` | ถูก | 26.4 |
| ci_sessions มีคอลัมน์อะไร | `ip_address` | ถูก | 35.8 |
| ความสัมพันธ์ระหว่างตารางมีอะไรบ้าง | `28` | ถูก | 52.9 |
| route ของ Channels มีอะไรบ้าง | `channels` | ถูก | 37.9 |
