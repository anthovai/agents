[pytest]
testpaths = tests
python_files = test_*.py
addopts =
    -v
    --tb=short
    --junitxml=reports/junit.xml
# Playwright's slow_mo plus the deliberate pauses make individual tests long;
# a short global timeout would fail watchable runs for being watchable.
markers =
    slow: takes tens of seconds by design
